{
  "name": "sudoku-core",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "description": "Pure, dependency-free sudoku generation, solving and difficulty rating (decision 3).",
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "build": "tsc -p tsconfig.build.json",
    "mine-examples": "tsc -p tsconfig.build.json && node dist/training/mine.js > src/training/examples.json && prettier --write src/training/examples.json"
  }
}
